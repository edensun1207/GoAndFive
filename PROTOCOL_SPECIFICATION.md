# GoAndFive 网络通信协议规范

## 版本信息

- **协议版本**: 1.0
- **文档版本**: 1.0
- **最后更新**: 2025-10-22

---

## 目录

1. [概述](#概述)
2. [传输层协议](#传输层协议)
3. [消息格式](#消息格式)
4. [消息类型](#消息类型)
5. [连接流程](#连接流程)
6. [游戏流程](#游戏流程)
7. [错误处理](#错误处理)
8. [协议扩展](#协议扩展)

---

## 概述

GoAndFive 采用基于 TCP/IP 的客户端-服务器架构，使用 JSON 格式进行数据交换。服务器监听两个端口，分别处理五子棋和围棋游戏。

### 设计原则

- **简洁性**: 使用 JSON 格式，易于理解和调试
- **可扩展性**: 支持添加新的消息类型
- **可靠性**: 基于 TCP 协议，保证消息顺序和完整性
- **实时性**: 即时转发玩家落子消息

---

## 传输层协议

### 网络配置

| 游戏类型 | 端口号 | 协议 | 最大连接数 |
|---------|--------|------|-----------|
| 五子棋  | 8825   | TCP  | 2         |
| 围棋    | 8826   | TCP  | 2         |

### 服务器地址

**默认配置**:
```
主机: 127.0.0.1 (localhost)
五子棋端口: 8825
围棋端口: 8826
```

**局域网配置**:
```
主机: 服务器局域网 IP (例如 192.168.1.100)
五子棋端口: 8825
围棋端口: 8826
```

### 连接参数

```cpp
// Qt 客户端连接示例
socket->connectToHost("127.0.0.1", 8825);
socket->waitForConnected(5000);  // 5秒超时
```

---

## 消息格式

### 基本格式

所有消息使用 **JSON** 格式，采用 **UTF-8** 编码。

```json
{
    "field1": "value1",
    "field2": value2,
    "field3": [array],
    "field4": {object}
}
```

### 编码规则

- **字符编码**: UTF-8
- **JSON 格式**: 紧凑格式（Compact）或格式化（Pretty）均可
- **数值类型**: 整数使用 int，浮点数使用 double
- **布尔类型**: true/false
- **数组**: 使用 JSON Array
- **对象**: 使用 JSON Object

### 示例

**紧凑格式** (推荐):
```json
{"game":"five","play":[7,7]}
```

**格式化**:
```json
{
    "game": "five",
    "play": [7, 7]
}
```

---

## 消息类型

### 1. 连接消息

#### 1.1 服务器 → 客户端：分配颜色

**发送时机**: 客户端连接成功后立即发送

**五子棋**:
```json
{
    "game": "five",
    "color": 1,
    "state": 0,
    "start": false
}
```

**围棋**:
```json
{
    "game": "go",
    "color": 1,
    "state": 0,
    "start": false
}
```

**字段说明**:

| 字段   | 类型    | 说明                           |
|--------|---------|--------------------------------|
| game   | string  | 游戏类型: "five" 或 "go"       |
| color  | int     | 棋子颜色: 1=黑棋, -1=白棋      |
| state  | int     | 玩家状态: 0=第一个, 1=第二个   |
| start  | boolean | 游戏是否开始                   |

---

### 2. 游戏开始消息

#### 2.1 服务器 → 所有客户端：游戏开始

**发送时机**: 第二个玩家加入后

```json
{
    "start": true
}
```

**字段说明**:

| 字段  | 类型    | 说明             |
|-------|---------|------------------|
| start | boolean | true=游戏开始    |

---

### 3. 落子消息

#### 3.1 客户端 → 服务器：发送落子

**发送时机**: 玩家点击棋盘落子时

**五子棋**:
```json
{
    "game": "five",
    "play": [7, 7]
}
```

**围棋**:
```json
{
    "game": "go",
    "play": [7, 7]
}
```

**字段说明**:

| 字段 | 类型   | 说明                      |
|------|--------|---------------------------|
| game | string | 游戏类型: "five" 或 "go"  |
| play | array  | [y坐标, x坐标]            |

**坐标系统**:
- 五子棋: 0-14 (15x15 棋盘)
- 围棋: 0-18 (19x19 棋盘)
- 原点 (0,0) 在左上角

#### 3.2 服务器 → 对手：转发落子

**发送时机**: 收到玩家落子后立即转发

```json
{
    "game": "five",
    "play": [7, 7]
}
```

---

### 4. 胜负消息（仅五子棋）

#### 4.1 服务器 → 所有客户端：游戏结束

**发送时机**: 检测到五子连珠后

```json
{
    "win": 1
}
```

**字段说明**:

| 字段 | 类型 | 说明                    |
|------|------|-------------------------|
| win  | int  | 获胜方颜色: 1=黑棋, -1=白棋 |

---

### 5. 吃子消息（仅围棋）

#### 5.1 服务器 → 客户端：提子通知

**发送时机**: 检测到无气棋子后

**单独发送给被吃方**:
```json
{
    "remove": [
        [5, 5],
        [5, 6],
        [6, 5]
    ]
}
```

**完整消息（包含落子）**:
```json
{
    "game": "go",
    "play": [7, 7],
    "remove": [
        [5, 5],
        [5, 6]
    ]
}
```

**字段说明**:

| 字段   | 类型  | 说明                      |
|--------|-------|---------------------------|
| remove | array | 被吃棋子坐标列表          |
| game   | string| 游戏类型 (可选)           |
| play   | array | 落子位置 (可选)           |

---

## 连接流程

### 完整连接序列

```
客户端1                服务器                客户端2
   |                      |                      |
   |-- TCP连接 --------->|                      |
   |                      |                      |
   |<-- 分配颜色 ---------|                      |
   |    {"color":1,       |                      |
   |     "start":false}   |                      |
   |                      |                      |
   |                      |<-- TCP连接 ----------|
   |                      |                      |
   |                      |--- 分配颜色 -------->|
   |                      |    {"color":-1,      |
   |                      |     "start":false}   |
   |                      |                      |
   |<-- 游戏开始 ---------|--- 游戏开始 -------->|
   |    {"start":true}    |    {"start":true}    |
   |                      |                      |
```

### 流程说明

1. **第一个客户端连接**
   - 建立 TCP 连接
   - 服务器分配黑棋（color: 1）
   - 设置 start: false，等待第二个玩家

2. **第二个客户端连接**
   - 建立 TCP 连接
   - 服务器分配白棋（color: -1）
   - 向两个客户端都发送游戏开始消息

3. **游戏开始**
   - 黑棋玩家先手
   - 开始轮流落子

---

## 游戏流程

### 五子棋游戏流程

```
黑棋玩家              服务器              白棋玩家
   |                      |                      |
   |-- 落子 [7,7] ------->|                      |
   |    {"game":"five",   |                      |
   |     "play":[7,7]}    |                      |
   |                      |                      |
   |                      |--- 转发落子 -------->|
   |                      |    {"game":"five",   |
   |                      |     "play":[7,7]}    |
   |                      |                      |
   |                      |<-- 落子 [8,7] -------|
   |                      |                      |
   |<-- 转发落子 ---------|                      |
   |                      |                      |
   |-- 落子 [6,7] ------->|                      |
   |                      |                      |
   |      ... (重复) ...  |                      |
   |                      |                      |
   |-- 落子 [9,7] ------->|                      |
   |    (五子连珠!)       |                      |
   |                      |                      |
   |<-- 胜负消息 ---------|--- 胜负消息 -------->|
   |    {"win":1}         |    {"win":1}         |
   |                      |                      |
```

### 围棋游戏流程（含吃子）

```
黑棋玩家              服务器              白棋玩家
   |                      |                      |
   |-- 落子 [5,5] ------->|                      |
   |                      |                      |
   |                      |--- 转发落子 -------->|
   |                      |                      |
   |                      |<-- 落子 [5,6] -------|
   |                      |                      |
   |<-- 转发落子 ---------|                      |
   |                      |                      |
   |      ... (重复) ...  |                      |
   |                      |                      |
   |-- 落子 [5,7] ------->|                      |
   |    (吃掉白子!)       |                      |
   |                      |                      |
   |                      |--- 吃子消息 -------->|
   |                      |    {"remove":        |
   |                      |     [[5,6]]}         |
   |                      |                      |
   |<-- 转发落子 ---------|                      |
   |                      |                      |
```

---

## 错误处理

### 连接错误

| 错误情况           | 处理方式                        |
|--------------------|---------------------------------|
| 服务器未启动       | 显示错误提示，等待重试          |
| 连接超时           | 5秒后超时，提示用户             |
| 玩家已满（2人）    | 拒绝连接，返回错误消息          |

### 数据错误

| 错误情况           | 处理方式                        |
|--------------------|---------------------------------|
| JSON 解析失败      | 忽略该消息，记录日志            |
| 坐标越界           | 忽略该消息，记录日志            |
| 无效的游戏类型     | 忽略该消息，记录日志            |

### 游戏逻辑错误

| 错误情况           | 处理方式                        |
|--------------------|---------------------------------|
| 非法落子（已有棋子）| 客户端拦截，不发送给服务器      |
| 不是当前回合       | 客户端拦截，不允许落子          |
| 游戏未开始         | 客户端拦截，等待第二个玩家      |

---

## 协议扩展

### 扩展消息类型

#### 聊天消息（建议）

**客户端 → 服务器**:
```json
{
    "type": "chat",
    "message": "Good game!"
}
```

**服务器 → 对手**:
```json
{
    "type": "chat",
    "player": 1,
    "message": "Good game!"
}
```

#### 悔棋请求（建议）

**客户端 → 服务器**:
```json
{
    "type": "undo_request"
}
```

**服务器 → 对手**:
```json
{
    "type": "undo_request",
    "player": 1
}
```

**对手 → 服务器**:
```json
{
    "type": "undo_response",
    "approved": true
}
```

#### 认输消息（建议）

**客户端 → 服务器**:
```json
{
    "type": "resign"
}
```

**服务器 → 双方**:
```json
{
    "type": "game_over",
    "reason": "resign",
    "winner": -1
}
```

#### 计时消息（建议）

**服务器 → 客户端**:
```json
{
    "type": "time_update",
    "player1_time": 300,
    "player2_time": 285
}
```

---

## 消息序列化

### Qt 实现示例

#### 发送消息

```cpp
// 构建 JSON 对象
QJsonObject obj;
obj.insert("game", "five");
obj.insert("play", QJsonArray({7, 7}));

// 序列化
QJsonDocument doc(obj);
QByteArray data = doc.toJson(QJsonDocument::Compact);

// 发送
socket->write(data);
socket->flush();
```

#### 接收消息

```cpp
// 读取数据
QByteArray data = socket->readAll();

// 解析 JSON
QJsonParseError error;
QJsonDocument doc = QJsonDocument::fromJson(data, &error);

if (error.error == QJsonParseError::NoError) {
    QJsonObject obj = doc.object();
    
    // 处理消息
    if (obj.contains("play")) {
        QJsonArray play = obj["play"].toArray();
        int y = play[0].toInt();
        int x = play[1].toInt();
        // ...
    }
} else {
    qWarning() << "JSON parse error:" << error.errorString();
}
```

---

## 协议版本控制

### 版本号格式

```
主版本号.次版本号.修订号
例如: 1.0.0
```

### 版本兼容性

| 主版本号 | 次版本号 | 修订号  | 兼容性              |
|---------|----------|---------|---------------------|
| 变化    | -        | -       | 不兼容，需要更新     |
| -       | 变化     | -       | 向后兼容，添加新功能 |
| -       | -        | 变化    | 完全兼容，修复 bug   |

### 版本协商（建议）

**连接时发送版本信息**:
```json
{
    "protocol_version": "1.0.0",
    "client_version": "1.0.0"
}
```

**服务器响应**:
```json
{
    "protocol_version": "1.0.0",
    "server_version": "1.0.0",
    "compatible": true
}
```

---

## 安全考虑

### 当前实现

- ✅ 限制最大连接数（2 个玩家）
- ✅ 使用 TCP 保证数据完整性
- ⚠️ 未实现身份验证
- ⚠️ 未实现数据加密
- ⚠️ 未实现防作弊机制

### 安全建议

1. **身份验证**: 添加登录系统
2. **数据加密**: 使用 SSL/TLS
3. **输入验证**: 服务器端验证所有输入
4. **防作弊**: 服务器端维护游戏状态
5. **速率限制**: 限制消息发送频率

---

## 性能优化

### 消息批量处理

对于频繁的小消息，可以批量发送：

```json
{
    "batch": [
        {"type": "play", "data": [7, 7]},
        {"type": "play", "data": [8, 7]},
        {"type": "play", "data": [9, 7]}
    ]
}
```

### 压缩

对于大量数据，可以使用 gzip 压缩：

```cpp
QByteArray compressed = qCompress(data);
socket->write(compressed);
```

---

## 调试和测试

### 日志格式

```
[2025-10-22 14:30:45] [INFO] Client connected: 127.0.0.1:52341
[2025-10-22 14:30:45] [SEND] {"game":"five","color":1,"state":0,"start":false}
[2025-10-22 14:30:52] [RECV] {"game":"five","play":[7,7]}
[2025-10-22 14:30:52] [SEND] {"game":"five","play":[7,7]}
```

### 测试工具

推荐使用以下工具测试协议：

1. **Wireshark**: 抓包分析网络流量
2. **netcat**: 模拟客户端连接
3. **Qt Network Protocol Analyzer**: Qt 内置工具

### 测试命令

```bash
# 使用 netcat 连接服务器
nc 127.0.0.1 8825

# 发送 JSON 消息（手动输入）
{"game":"five","play":[7,7]}
```

---

## 附录

### A. 完整消息列表

| 消息类型       | 方向          | 必需字段          | 可选字段  |
|---------------|---------------|-------------------|----------|
| 连接响应       | S→C           | game, color, state, start | -        |
| 游戏开始       | S→C           | start             | -        |
| 落子请求       | C→S           | game, play        | -        |
| 落子转发       | S→C           | game, play        | -        |
| 胜负通知       | S→C           | win               | -        |
| 吃子通知       | S→C           | remove            | game, play |

### B. 错误代码（建议）

| 代码 | 说明              |
|------|-------------------|
| 1000 | 正常关闭          |
| 4001 | 无效的消息格式    |
| 4002 | 无效的坐标        |
| 4003 | 非法落子          |
| 4004 | 不是当前回合      |
| 5001 | 服务器内部错误    |

---

**协议版本**: 1.0  
**文档版本**: 1.0  
**最后更新**: 2025-10-22  
**维护者**: GoAndFive 开发团队
