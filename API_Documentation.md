# GoAndFive 项目 API 文档

## 项目概述

GoAndFive 是一个基于Qt的网络棋类游戏项目，支持围棋（Go）和五子棋（Five）两种游戏模式。项目采用客户端-服务器架构，允许同一局域网内的玩家进行对战。

### 项目结构
```
/workspace/
├── Go-Five/          # 客户端代码
│   ├── board.h/.cpp  # 棋盘组件
│   ├── five.h/.cpp   # 五子棋游戏逻辑
│   ├── go.h/.cpp     # 围棋游戏逻辑
│   ├── welcome.h/.cpp # 欢迎界面
│   └── main.cpp      # 客户端入口
└── server/           # 服务器代码
    ├── server.h/.cpp # 服务器逻辑
    └── main.cpp      # 服务器入口
```

---

## 客户端 API 文档

### 1. Board 类 - 棋盘组件

#### 类描述
`Board` 类继承自 `QMainWindow`，负责棋盘的绘制、棋子的显示和用户交互。

#### 公共接口

##### 构造函数
```cpp
explicit Board(int n);
```
**参数：**
- `n`: 棋盘大小参数
  - 14: 五子棋棋盘 (15x15)
  - 18: 围棋棋盘 (19x19)

**功能：** 创建指定大小的棋盘窗口

##### 公共方法

```cpp
void setColor(int n);
```
**参数：**
- `n`: 玩家颜色 (-1: 白棋, 1: 黑棋)

**功能：** 设置当前玩家的棋子颜色

```cpp
void receivePiece(int y, int x, int color);
```
**参数：**
- `y`: 行坐标 (0-18)
- `x`: 列坐标 (0-18)  
- `color`: 棋子颜色 (-1: 白棋, 1: 黑棋)

**功能：** 在指定位置放置棋子并刷新显示

```cpp
void remove(int y, int x);
```
**参数：**
- `y`: 行坐标
- `x`: 列坐标

**功能：** 移除指定位置的棋子（用于围棋提子）

##### 公共成员变量

```cpp
bool isTurn;    // 是否轮到当前玩家
bool isStart;   // 游戏是否已开始
int color;      // 当前玩家颜色
```

##### 信号

```cpp
void putPiece(int y, int x);
```
**触发条件：** 玩家点击棋盘放置棋子时
**参数：**
- `y`: 行坐标
- `x`: 列坐标

#### 使用示例

```cpp
// 创建五子棋棋盘
Board* board = new Board(14);
board->show();

// 设置玩家为黑棋
board->setColor(1);

// 连接信号槽
connect(board, SIGNAL(putPiece(int,int)), this, SLOT(handlePiece(int,int)));

// 在(5,5)位置放置白棋
board->receivePiece(5, 5, -1);
```

---

### 2. Five 类 - 五子棋游戏

#### 类描述
`Five` 类继承自 `QMainWindow`，实现五子棋游戏的网络通信和游戏逻辑。

#### 公共接口

##### 构造函数
```cpp
Five(QWidget *parent = 0);
```
**功能：** 创建五子棋游戏实例，自动连接到服务器

##### 析构函数
```cpp
~Five();
```

#### 私有槽函数

```cpp
void receiveMsg();
```
**功能：** 接收服务器消息并处理游戏状态更新

```cpp
void sendMsg(int y, int x);
```
**参数：**
- `y`: 行坐标
- `x`: 列坐标

**功能：** 向服务器发送落子信息

#### 网络协议

##### 客户端发送消息格式
```json
{
    "game": "five",
    "play": [y, x]
}
```

##### 服务器响应消息格式
```json
{
    "color": 1,           // 玩家颜色
    "start": true,        // 游戏开始状态
    "play": [y, x],       // 对手落子位置
    "win": 1              // 获胜玩家颜色
}
```

#### 使用示例

```cpp
// 创建五子棋游戏
Five* game = new Five();
// 游戏会自动连接到 127.0.0.1:8825
```

---

### 3. Go 类 - 围棋游戏

#### 类描述
`Go` 类继承自 `QMainWindow`，实现围棋游戏的网络通信和游戏逻辑。

#### 公共接口

##### 构造函数
```cpp
Go(QWidget *parent = 0);
```
**功能：** 创建围棋游戏实例，自动连接到服务器

##### 析构函数
```cpp
~Go();
```

#### 私有槽函数

```cpp
void receiveMsg();
```
**功能：** 接收服务器消息，处理落子和提子逻辑

```cpp
void sendMsg(int y, int x);
```
**参数：**
- `y`: 行坐标
- `x`: 列坐标

**功能：** 向服务器发送落子信息

#### 网络协议

##### 客户端发送消息格式
```json
{
    "game": "go",
    "play": [y, x]
}
```

##### 服务器响应消息格式
```json
{
    "color": 1,           // 玩家颜色
    "start": true,        // 游戏开始状态
    "play": [y, x],       // 对手落子位置
    "remove": [[x1,y1], [x2,y2]]  // 被提取的棋子位置数组
}
```

#### 使用示例

```cpp
// 创建围棋游戏
Go* game = new Go();
// 游戏会自动连接到 127.0.0.1:8826
```

---

### 4. welcome 类 - 欢迎界面

#### 类描述
`welcome` 类继承自 `QMainWindow`，提供游戏选择界面。

#### 公共接口

##### 构造函数
```cpp
welcome();
```
**功能：** 创建欢迎界面，包含游戏选择按钮

##### 析构函数
```cpp
~welcome();
```

#### 私有槽函数

```cpp
void startFive();
```
**功能：** 启动五子棋游戏

```cpp
void startGo();
```
**功能：** 启动围棋游戏

#### 界面布局

- **GO 按钮**: 位置(100,20)，大小(100,50)
- **FIVE 按钮**: 位置(100,80)，大小(100,50)  
- **QUIT 按钮**: 位置(100,140)，大小(100,50)

#### 使用示例

```cpp
// 创建并显示欢迎界面
welcome w;
w.show();
```

---

## 服务器 API 文档

### Server 类 - 游戏服务器

#### 类描述
`Server` 类继承自 `QMainWindow`，实现多人游戏的服务器逻辑，支持同时处理五子棋和围棋游戏。

#### 数据结构

##### User 结构体
```cpp
struct User {
    QTcpSocket *tcpSocket;  // 网络连接
    QByteArray byte;        // 消息缓冲区
    int color;              // 玩家颜色 (-1: 白棋, 1: 黑棋)
    bool isTurn;            // 是否轮到该玩家
};
```

#### 公共接口

##### 构造函数
```cpp
explicit Server(QWidget *parent = 0);
```
**功能：** 创建服务器实例，监听端口8825(五子棋)和8826(围棋)

##### 析构函数
```cpp
~Server();
```

##### 公共方法

```cpp
void init();
```
**功能：** 初始化游戏状态和用户计数

```cpp
bool checkWin_Five(int y, int x);
```
**参数：**
- `y`: 最后落子的行坐标
- `x`: 最后落子的列坐标

**返回值：** 是否有玩家获胜
**功能：** 检查五子棋胜负条件（五子连珠）

```cpp
void remove_Go(int c);
```
**参数：**
- `c`: 刚落子的颜色

**功能：** 处理围棋提子逻辑

#### 公共槽函数

```cpp
void sendMsg();
```
**功能：** 转发玩家消息给对手

```cpp
void receiveMsg();
```
**功能：** 接收并处理客户端消息

```cpp
void newListen();
```
**功能：** 开始监听客户端连接

```cpp
void acceptFiveConnection();
```
**功能：** 接受五子棋客户端连接

```cpp
void acceptGoConnection();
```
**功能：** 接受围棋客户端连接

```cpp
void userOff();
```
**功能：** 处理用户断线

#### 私有方法

```cpp
bool hasQi(int x, int y, int c);
```
**参数：**
- `x`, `y`: 棋子坐标
- `c`: 棋子颜色

**返回值：** 该位置的棋子是否有气
**功能：** 检查围棋棋子的气

```cpp
bool hasQiOfColor(int c, int &p, int &q);
```
**参数：**
- `c`: 棋子颜色
- `p`, `q`: 返回无气棋子的坐标

**返回值：** 该颜色的棋子是否都有气
**功能：** 检查某种颜色的所有棋子是否有气

```cpp
void initFlag();
```
**功能：** 初始化访问标记数组

```cpp
void killPiece(int x, int y, int c);
```
**参数：**
- `x`, `y`: 棋子坐标
- `c`: 棋子颜色

**功能：** 递归提取无气的棋子

#### 服务器配置

- **五子棋端口**: 8825
- **围棋端口**: 8826
- **最大玩家数**: 每个游戏2人
- **监听地址**: 127.0.0.1 (本地)

#### 使用示例

```cpp
// 启动游戏服务器
QApplication app(argc, argv);
Server server;
server.show();
return app.exec();
```

---

## 游戏规则说明

### 五子棋规则
1. 15x15棋盘
2. 黑棋先行
3. 五子连珠获胜（横、竖、斜任意方向）
4. 禁止悔棋

### 围棋规则
1. 19x19棋盘
2. 黑棋先行
3. 无气棋子被提取
4. 支持打劫规则

---

## 网络通信协议

### 连接建立
- 客户端连接到服务器指定端口
- 服务器分配玩家颜色和游戏状态
- 等待第二个玩家加入开始游戏

### 消息格式
所有消息使用JSON格式，通过TCP传输。

### 错误处理
- 网络断线自动重连
- 非法落子位置检查
- 游戏状态同步验证

---

## 编译和运行

### 依赖项
- Qt 5.x 或更高版本
- C++11 支持的编译器

### 编译步骤

#### 客户端
```bash
cd Go-Five
qmake cppFinal.pro
make
```

#### 服务器
```bash
cd server
qmake chessServer.pro
make
```

### 运行步骤
1. 首先启动服务器
2. 启动客户端程序
3. 选择游戏模式（GO或FIVE）
4. 等待第二个玩家加入

---

## 扩展开发指南

### 添加新游戏模式
1. 创建新的游戏类继承自QMainWindow
2. 实现网络通信接口
3. 在服务器添加对应的端口监听
4. 在欢迎界面添加选择按钮

### 自定义棋盘大小
修改Board构造函数参数，调整相应的游戏逻辑边界检查。

### 网络优化
- 添加消息压缩
- 实现断线重连
- 添加心跳检测

---

## 常见问题

### Q: 客户端无法连接服务器
A: 检查服务器是否启动，防火墙设置，端口是否被占用

### Q: 游戏中途断线
A: 重启客户端程序，服务器会自动清理断线连接

### Q: 棋子显示异常
A: 检查Qt版本兼容性，确保图形驱动正常

---

## 版本历史

- **v1.0**: 基础五子棋和围棋功能
- 支持本地网络对战
- 基本的胜负判定

---

## 许可证

本项目为C++课程设计作业，仅供学习参考使用。